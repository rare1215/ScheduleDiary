<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_diary"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_diary"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScheduleDiary">

        <activity
            android:name=".activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.RegisterActivity" android:exported="false"/>
        <activity android:name=".activity.MainActivity" android:exported="false"/>
        <activity android:name=".activity.DiaryDetailActivity" android:exported="false"/>
        <activity android:name=".activity.PageEditActivity" android:exported="false"/>

    </application>

</manifest>
